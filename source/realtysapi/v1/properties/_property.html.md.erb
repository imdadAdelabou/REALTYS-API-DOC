# Property Published Module

## Add Property
```dart
var headers = {
  'Content-Type': 'application/x-www-form-urlencoded'
};
var request = http.Request('POST', Uri.parse('https://realtys-api.herokuapp.com/api/realtys/user/add-property'));
request.bodyFields = {
  'email': 'junior.medehou00@gmail.com',
  'designation': 'Maison bricolé',
  'value': '150000',
  'location': 'Pharmacie Midombo',
  'isAuthentified': 'false'
};
request.headers.addAll(headers);

http.StreamedResponse response = await request.send();

if (response.statusCode == 200) {
  print(await response.stream.bytesToString());
}
else {
  print(response.reasonPhrase);
}
```

> This is the response we should get:

```json
{
    "msg": "Property add corectly"
}
```

### Request Information
|Category|Value|
|----------|--------|
|Http request| `POST`|
|URL|https://realtys-api.herokuapp.com/api/realtys/user/add-property|

### Headers
No headers required

### Input Schema for requests
|Field|Meaning|
|-----|--------|
|email| email of the user who wants to add a property|
|designation| designation of the property|
|value| value of the property|
|location| location of the property|
|isAuthentified| boolean to know if the property is certified or not|

### Output Schema of request
|Field|Meaning|
|-----|--------|
|status| return `200` if the property has been added correctly|
|msg| The result of the request|


